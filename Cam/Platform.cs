namespace StreamRC.Cam {

    /// <summary>
    /// platform which camera filter is registered to
    /// </summary>
    public enum Platform {

        /// <summary>
        /// 32-bit
        /// </summary>
        X86,

        /// <summary>
        /// 64-bit
        /// </summary>
        X64
    }
}